{
  "name": "onoffice-middleware",
  "version": "1.0.0",
  "description": "onOffice API Middleware mit automatischer HMAC-Authentifizierung",
  "main": "api/onoffice.js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.x"
  }
}